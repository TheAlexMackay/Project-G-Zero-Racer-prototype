/** Off-track shoulder width (world units) beyond the road edge before a solid wall stops the ship. */
export const WALL_MARGIN = 60;

/** Visual thickness of the wall stripe painted at the shoulder's outer edge. */
export const WALL_STRIPE_WIDTH = 6;
